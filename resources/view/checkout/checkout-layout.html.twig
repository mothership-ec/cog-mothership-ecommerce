<!DOCTYPE html>
<html>
	<head>
		<title>Checkout - {{ stageTitle }}</title>

		{% include 'Message:Mothership:Ecommerce::checkout:includes:js-block' %}
	</head>
	<body>
	{% for key,types in app.session.getFlashBag.all %}
		<div class="feedback {{ key }}">
			{% for message in types %}
				<p>{{ message|raw }}</p>
			{% endfor %}
		</div>
	{% endfor %}

	{% block content %}{% endblock %}

	</body>
</html>