{% extends '::fulfillment' %}

{% block process %}
	<h2>{{ heading }}</h2>
	{{ form_start(form) }}
		<ul>
			{% for key, item in items %}
				<li>
					<h3>{{ item.productName }} - {{ item.id }}</h3>
					{{ form_widget(form['choices'][key]) }}
				</li>
			{% endfor %}
		</ul>

		{{ form_rest(form) }}

		<button type="submit" class="button small save">{{ action }}</button>


		{% if pickingSlips | length > 0 %}
			<h3>View packing slips</h3>
			<ul>
				{% for documentID, pickingSlip in pickingSlips %}
					<li><a href="{{ url('ms.ecom.fulfillment.picking.view', {'documentID': documentID, 'orderID': pickingSlip}) }}" target="_blank">
						{{ documentID }}
					</a></li>
				{% endfor %}
			</ul>
		{% endif %}

	{{ form_end(form) }}
{% endblock %}